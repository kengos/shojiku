version: 1
title: Order
type: object
properties:
  customer:
    type: object
    properties:
      name: { type: string, title: Customer name, example: Shojikudo Trading }
  order:
    type: object
    properties:
      placedAt: { type: string, format: date, title: Placed at }
      total: { type: integer, format: currency, currency: JPY, title: Order total }
