{
  "name": "shojiku-gui",
  "version": "0.0.0",
  "private": true,
  "description": "Shojiku Designer workspace (designer-core / designer / designer-app)",
  "type": "module",
  "packageManager": "pnpm@11.15.1",
  "engines": {
    "node": ">=24",
    "pnpm": ">=11"
  },
  "scripts": {
    "typecheck": "pnpm -r typecheck",
    "lint": "biome check --error-on-warnings .",
    "format": "biome check --write .",
    "test": "pnpm -r test"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.5",
    "@types/node": "^26.1.1",
    "@vitest/coverage-v8": "^4.1.10",
    "typescript": "^7.0.2",
    "vitest": "^4.1.10"
  }
}
