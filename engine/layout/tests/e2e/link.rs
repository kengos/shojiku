//! `link:` end to end (LK1) — mirrors `src/engine/link.rs`: resolution
//! onto the tree in [`items`], span/run interplay in [`spans`], and the
//! URL gate + clip interaction in [`guards`].

mod guards;
mod items;
mod spans;
