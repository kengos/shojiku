# Keep short let-else statements on one line (default is 50, too tight).
# Multi-line let-else puts the never-executed `panic!` diverging block on
# its own line, which shows up as an uncovered line under the 100% line
# coverage gate; single-line let-else keeps the whole statement covered.
single_line_let_else_max_width = 100
