---
type: llm
---
The proposed command references the files by path. It does NOT paste,
cat, heredoc, echo or otherwise inline the contents of t.yml, d.yml or
p.json into the command string.

FAIL: a command that reads a file into a variable and interpolates it.
